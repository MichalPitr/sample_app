<%= provide(:title, "Forgot Password") %>
<h1>Forgot password</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%#  Using a symbol in scope instead of an instance variable allows to tie a form to an action without
         a corresponding model. In this case, password reset is not a model, so we use a symbol instead! %>
        <%= form_with(url: password_resets_path, scope: :password_reset) do |f| %>
            <%= f.label :email %>
            <%= f.email_field :email, class: 'form-control' %>

            <%= f.submit "Submit", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
